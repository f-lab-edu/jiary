import { style } from '@vanilla-extract/css';

export const wrapper = style({
  flex: 1,
});

export const inputButton = style({
  width: '100%',
  display: 'flex',
  padding: '8px 12px',
  borderRadius: 15,
  fontSize: 16,
});
